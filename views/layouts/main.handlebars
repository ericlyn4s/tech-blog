<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<!-- Header has several buttons: home, dashboard, and login/logout depending on status -->
<body>
  <header>
    <nav>
      <a href="/">Home</a>
      {{#if loggedIn}}
      <a href="/createpost">Dashboard</a>
      <button id="logout">Logout</button>
      {{else}}
      <a href="/login">Login</a>
      {{/if}}
    </nav>
    <div id="title-button">
      <h1>The Tech Blog</h1>
    </div>

  </header>
  
  <!-- Checks if user is signed in or not, displays this section accordingly -->
  <main>
    <div class="post">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}

</body>

</html>