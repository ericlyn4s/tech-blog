<section class="card">

<header>
    <div>{{post.title}}</div>
    <div id="poster">Posted on {{format_time post.createdAt}}</div>
</header>

<div class="card-body">
    <p>{{post.content}}</p>
</div>

</section>

<section class="card">
<form class="form comment-form comment-box" id="comment-form">
    <div class="form-group">
        <label for="comment-body" class="form-group">Add a comment:</label>
        <input class="form-input" type="comment" id="comment-body" />
    </div>
    <div class="form-group">
        <button class="btn btn-primary" type="submit">Submit</button>
    </div>
</form>
</section>

{{#each post.comments as |comment|}}
{{> comment-details}}
{{/each}}

</section>
<script src="/js/comment.js"></script>



