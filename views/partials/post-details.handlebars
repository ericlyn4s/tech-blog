<section class="card">

<a href="/">
    <header>
      <div>{{title}}</div>
      <div id="poster">Posted on {{createdAt}}</div>
    </header>
      <div class="card-body">
        <p>{{content}}</p>
      </div>
</a>

</section>